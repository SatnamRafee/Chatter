@model IEnumerable<Chatter.Models.ChatViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Chat.PublishDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Chat.ChatBody)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ApplicationUser.ChatName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Chat.PublishDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Chat.ChatBody)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ApplicationUser.ChatName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Chat.ChatID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Chat.ChatID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Chat.ChatID })
        </td>
    </tr>
}

</table>
